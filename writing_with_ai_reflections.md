# Writing With AI Reflections

> 这份文档整理一轮关于 RAG、长上下文 LM、skill、提示词能力和写作训练的对话思考。形式采用问答，不追求结论永远正确，而是保留问题推进的过程。

## Q1：如果 LM 已经有 1M 上下文窗口，RAG 还必要吗？

**问题**

当一个语言模型已经可以吃下一整本书，传统 RAG 的必要性会被削弱。对于单本、静态、规模不大的资料，与其做解析、切块、向量化、索引、召回、重排，不如直接把完整资料交给 LM。

这带来一个反问：

```text
如果知识库并不大，为什么不直接把资料扔给 LM？
```

**回答**

这个怀疑是合理的，而且应该成为当前路线中的强基线。

RAG 不应该被默认当成正确架构。它解决的问题主要是：

- 内容太大，放不进上下文。
- 文档很多，无关信息太多，需要先找候选证据。
- 同一知识库会被反复查询，索引成本可以摊薄。
- 知识需要增量更新，不适合每次重送全量上下文。
- 答案必须稳定引用页码、段落、图片或表格。
- 数据有权限、租户、范围边界，不能一次性全部暴露。
- 原始资料中的图片、表格、架构图需要先被转写成文本证据。

如果只有一本书，而且上下文窗口足够，那么最强 baseline 应该是：

```text
clean document.md + image captions + long-context LM
```

在这个基线没有跑过之前，不应该急着证明 RAG 有价值。

## Q2：那是不是构建一个 `rag.skill` 比构建完整 RAG 系统更有用？

**问题**

如果一本书可以直接交给 LM，那么 RAG 的复杂 pipeline 可能不是当前最有效的投入。相比之下，是否应该构建一个 skill，让 LM 学会如何使用已有文档、已有上下文来回答问题？

**回答**

是的，当前更有价值的方向可能不是先做完整 RAG 产品，而是先做一个更轻的文档问答 skill。

不过它不一定应该叫 `rag.skill`。因为此时核心并不是检索，而是：

```text
如何让 LM 严格、有效、可复盘地使用一份技术文档回答问题。
```

更准确的名字可能是：

```text
technical_doc_qa.skill
long_context_doc_qa.skill
```

这个 skill 应该负责规定：

- 如何读取 `document.md`。
- 如何利用目录、章节、页码、图片 caption。
- 回答时如何引用证据。
- 找不到答案时如何拒答。
- 遇到 image-helpful / image-required 问题时如何处理图片 caption。
- 如何把长上下文回答与 RAG 回答做对照。

所以简化后的路线可以是：

```text
Layer 1: parser
PDF -> document.md + images + image captions

Layer 2: long-context QA skill
把 document.md / caption / 目录 / 引用规则交给 LM

Layer 3: RAG baseline
仅作为对照实验，比较 text-only RAG / image-aware RAG 是否真的有收益
```

## Q3：这是否意味着当前任务本质上是“基于已有知识和上下文完成任务”？

**问题**

很多任务并不一定需要复杂系统，而是已经有足够上下文，关键在于如何让 LM 正确使用这些上下文。此时真正重要的是有效提示词和足够聪明的 LM。

**回答**

是的。这类任务的核心可以理解为：

```text
已有上下文 + 明确任务 + 合适提示词 + 强 LM
```

skill 的价值也只会在一种情况下真正显现：它对任务的解决方案分析得足够透彻。

如果 skill 只是几句泛泛的角色设定，它的价值有限；如果 skill 沉淀了任务边界、上下文读取方法、证据使用规则、输出契约和失败处理方式，它就会成为可复用的工程资产。

换句话说，skill 不是 prompt 的包装壳，而是对一类任务的可执行理解。

## Q4：模型越来越强，提示词能力是不是越来越不重要？

**问题**

随着 LM 能力提升，很多早期 prompt trick 的作用确实变小了。模型更能理解自然语言，也更能补全模糊意图。那提示词能力还有必要专门训练吗？

**回答**

提示词的低级技巧会变得不重要，但高级提示词能力仍然必要。

早期的 prompt 经常像“咒语”：加某些固定句式、角色、格式，就能明显改变效果。强模型出现后，这类技巧的边际价值会下降。

但真正重要的 prompt 能力并不是咒语，而是：

```text
问题定义能力
+ 上下文组织能力
+ 任务分解能力
+ 输出约束能力
+ 评估与迭代能力
```

模型越强，它越能接住复杂任务；但前提是人能把任务说清楚。好的提示词不是操控模型，而是把模糊需求压缩成 LM 可以执行的任务接口。

## Q5：怎样提升提示词能力？

**问题**

如果提示词能力不是背模板，那应该如何练习？

**回答**

可以把提示词能力拆成五层训练。

### 1. 任务定义

先回答：

```text
我要模型做什么？
为了谁做？
什么算好结果？
什么是不允许的？
```

例如，“总结文档”很弱；更好的任务定义是：

```text
面向准备实现该功能的 Python 开发者，总结文档中的 API 调用流程、参数约束、错误处理，并引用章节来源。
```

### 2. 上下文组织

长上下文时代，不是把所有内容塞进去就够了，而是要让模型知道怎么读。

好的上下文通常包括：

- 文档标题。
- 目录结构。
- 正文。
- 图片 caption。
- 证据编号。
- 页码或章节。
- 引用规则。

这也是 `document.md + image captions + skill` 有价值的原因。它不是检索，而是帮助 LM 更可靠地使用已有上下文。

### 3. 过程约束

不要只让模型自由发挥，而要给它一个可执行流程：

```text
先判断问题类型
再定位相关章节
再提取证据
再回答
最后说明不确定性
```

这里不需要让模型展示冗长思维链，需要的是可执行步骤和可检查结果。

### 4. 输出契约

输出不稳定，很多时候不是模型不聪明，而是接口没有定义清楚。

可以要求输出包含：

```text
回答：
证据：
引用：
不确定性：
下一步建议：
```

或者使用 JSON、Markdown 表格、固定小节。输出格式越贴近后续使用，prompt 越像一个可靠接口。

### 5. 评估闭环

真正提升 prompt 能力的方法不是反复改一句话，而是固定问题集反复比较：

```text
同一批问题
同一份文档
不同 prompt
比较正确性、证据质量、拒答能力、简洁度
```

没有评估，prompt 优化很容易变成“感觉变好了”。

## Q6：这是不是意味着要训练写作能力？

**问题**

如果提示词本质上是任务接口，那么写好提示词就需要写作能力。可是已经很久没有认真写长文本了，也越来越依赖 AI 帮忙整理表达。

**回答**

是的，但这里的写作能力不是文学写作，而是工程化表达能力。

它的核心是：

```text
把脑子里的模糊判断
变成别人和 LM 都能理解、执行、复盘的文本。
```

可以拆成三种能力：

```text
1. 想清楚：我到底在判断什么？
2. 说清楚：别人或 LM 如何理解并行动？
3. 留下来：以后如何复盘、迭代、复用？
```

与 AI 协作时，写作变得更重要，而不是更不重要。因为 LM 很聪明，会自动补全模糊部分；但它补出来的未必就是你真正想要的。你写得越清楚，它越能接住；你写得越含混，它越容易替你做未经确认的假设。

## Q7：如何在和 AI 协作中训练自己的写作，而不是变成完全依赖 AI？

**问题**

如果每次都让 AI 改写和总结，是否会削弱自己的表达能力？应该怎样协作才是真正训练自己？

**回答**

可以采用一种轻量的协作式写作训练法：

```text
1. 先自己写一段原始想法，不求完整。
2. 让 AI 提炼核心判断。
3. 让 AI 指出含混、跳跃、缺证据的地方。
4. 让 AI 给一个更清晰版本，并解释为什么这样改。
5. 自己再改一版。
```

关键是不要把 AI 只当代写工具，而是当成写作教练和结构反馈器。

一个好的协作循环是：

```text
我的原始想法
-> AI 帮我看见结构
-> 我确认哪些是我的真实判断
-> AI 帮我重组表达
-> 我再吸收成自己的版本
```

这样训练的不是“让 AI 写得漂亮”，而是逐步提高自己组织判断、表达边界、定义任务的能力。

## Q8：文字的作用是否有边界？

**问题**

文字当然重要，但文字不是万能的。很多事情仅靠文字会损失直觉、结构、情绪、操作摩擦和真实反馈。那些仅通过文字就能处理好事情的人令人钦佩，但是否所有事情都应该追求“只靠文字解决”？

**回答**

文字有边界，这个判断很重要。

文字擅长：

- 抽象。
- 归纳。
- 说明规则。
- 传递约束。
- 复盘决策。
- 建立共识。

但文字不擅长完整承载：

- 视觉结构。
- 情绪细节。
- 交互体验。
- 实验结果。
- 系统动态。
- 身体经验和真实操作中的摩擦。

所以好的协作不是迷信文字，而是知道什么时候该用文字，什么时候该用图、代码、表格、日志、截图、实验数据来补足文字。

对这个项目来说，理想的表达不是纯文字，也不是纯代码，而是：

```text
文档说明问题
代码实现路径
数据验证判断
截图检查现象
日志保留过程
Markdown 沉淀复盘
```

这也正好对应 `pipelines_rag` 的长期路线：不是只相信 RAG，也不是只相信长上下文 LM，而是用最小实验判断什么时候该用什么复杂度。

## Q9：当前最简化的行动路线是什么？

**问题**

为了避免过度设计，现在应该怎么做？

**回答**

当前可以先不做完整 RAG 产品、不做复杂 agent workflow、不做 UI dashboard。只保留一个最小闭环：

```text
1. 选一份代表性技术文档。
2. 解析成 clean document.md。
3. 为关键图片补充 caption。
4. 写一个 long-context doc QA prompt / skill 草稿。
5. 准备 20–30 个固定问题。
6. 直接把 document.md + captions 给 LM。
7. 观察它能否正确回答、引用证据、拒绝无答案问题。
8. 再用简单 RAG 作为对照，而不是默认主线。
```

这条路线的好处是：

- 先验证长上下文 LM 的真实能力。
- 避免一开始陷入复杂 RAG 工程。
- 把提示词、写作、上下文组织和评估能力练起来。
- 如果后续发现长上下文不够，再自然升级到 RAG。

## Q10：多轮 agent 协作时，如何处理上下文膨胀？

**问题**

即使单次模型已经支持很长上下文，agent 协作完成一个项目时，多轮对话仍然会不断积累历史信息。需求讨论、方案取舍、代码改动、失败记录、临时想法、过期判断都会留在上下文里。

当上下文越来越接近上限时，即使没有完全爆掉，agent 的效果也可能下降：

- 重要约束被淹没。
- 旧决策和新决策混在一起。
- agent 更容易继承过期假设。
- 检索和推理成本升高。
- 回答看似连续，实际已经抓不住当前主线。

所以一个新的问题出现了：

```text
如何把长对话中的有效上下文，持续整理成可复用的项目记忆？
```

**回答**

这个思路很有价值。它可以叫做“上下文治理”，也可以看作 prompt 写作能力的一部分。

长上下文并不意味着可以无限堆上下文。1M 上下文解决的是“能不能放下”，不自动解决“模型能不能稳定抓住重点”。对 agent 协作来说，更好的策略不是把所有历史都留在对话里，而是定期把上下文压缩、整理、迁移到稳定文档中。

在这个项目里，最适合承载稳定上下文的是：

```text
AGENTS.md
CLAUDE.md
strata.md
deep_strata.md
writing_with_ai_reflections.md
rag_pdfs/plan.md
parsers/plan.md
rag_pdfs/logs.md
```

其中不同文件承担不同职责：

- `AGENTS.md`：给 agent 的长期操作规则，应该写稳定约束，不写一时想法。
- `CLAUDE.md`：通常作为兼容 shim，指向 canonical agent 规则。
- `strata.md` / `deep_strata.md`：项目方向、第一性原理、阶段性判断。
- `writing_with_ai_reflections.md`：关于协作、写作、prompt、上下文治理的反思。
- `rag_pdfs/plan.md` / `parsers/plan.md`：工程计划和模块边界。
- `rag_pdfs/logs.md`：真实运行记录，保持追加式，不用于写长篇路线判断。

一个轻量可执行的做法是：

```text
每隔一段重要协作
-> 回看本轮对话
-> 抽取仍然有效的决策、约束、术语、下一步
-> 删除临时情绪、过期假设、重复解释
-> 写入对应文档
-> 下一轮对话让 agent 先读这些文档，而不是依赖完整聊天历史
```

这本质上是在训练两种能力：

```text
1. 上下文压缩：从大量对话里提取真正有用的信息。
2. 指令写作：把有效信息写成 agent 能执行的规则和判断。
```

这比单纯“让 AI 总结一下聊天记录”更进一步。好的上下文整理不是摘要，而是重新判断哪些内容应该变成长期记忆，哪些只应该留在历史里。

### 建议的整理原则

可以用四类信息来判断是否值得写入长期文档：

```text
Decision：已经确认的决定。
Rule：以后要反复遵守的规则。
Rationale：为什么这么做，避免未来误解。
Next：下一步明确行动。
```

不建议写入长期文档的是：

- 临时猜测。
- 已经被推翻的方案。
- 一次性的工具输出细节。
- 没有行动意义的情绪性表达。
- 过细、很快会过期的实现细节。

更简洁的模板是：

```text
本轮新增判断：
- ...

已确认规则：
- ...

下一步：
- ...

不再采用：
- ...
```

### 对 agent 效果的影响

上下文整理得好，agent 的效果通常会自然变好。原因不是文档本身有魔法，而是它降低了协作中的歧义：

- agent 更容易知道当前项目的真实目标。
- agent 不必重复从长历史里猜测规则。
- 旧上下文被压缩成清晰约束。
- 新对话可以更短、更稳。
- 人也会被迫重新判断自己的想法是否成立。

所以这不仅是给 agent 减负，也是给自己训练：

```text
我到底决定了什么？
为什么决定？
这个决定应该放进哪份文档？
它是一条长期规则，还是一次性记录？
```

这个练习和 prompt 能力高度一致。prompt 是给模型的即时任务接口；`AGENTS.md`、`strata.md`、`plan.md` 这类文件，则是给未来协作的长期任务接口。

## Q11：这一轮思考的核心结论是什么？

**回答**

可以压缩成三句话：

```text
1. RAG 不是默认答案；在 1M 上下文时代，长上下文 LM 是必须先击败的强基线。
2. skill 的价值来自对任务的透彻分析，而不是提示词包装；当前更值得做 long-context technical doc QA skill。
3. 提示词能力的本质是工程化写作能力：定义任务、组织上下文、约束输出、评估结果，并知道文字什么时候不够。
4. 多轮 agent 协作需要上下文治理：把有效历史整理进稳定文档，控制对话上下文膨胀。
```

因此，接下来真正值得训练的是：

```text
写清楚问题
整理好上下文
设计可执行提示词
固定问题集评估
根据失败继续迭代
```

这不是远离 RAG，而是在为 RAG 找到更扎实的地基。
