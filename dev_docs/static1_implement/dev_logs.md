# static1 开发执行记录

> 文档归属：static1。2026-09-16 从 `static1/dev_logs.md` 迁入。本文件是此主题的唯一维护入口。正文中的源码路径以仓库根目录或明确标注的 static1 目录为基准，不以本文目录为基准。


## 2026-09-16 · 开发文档分层归档

【任务】将static1规划、设计、实现记录和历史资料集中到dev_docs，避免多个位置维护相同说明。不改应用代码，不整理无关项目笔记。

【实现情况】建立总导航、static1_plan、static1_design、static1_implement和deprecated；迁移正文并重定位相对链接。旧路径仅保留导航，README保留用户启动说明，AGENTS.md保持原作用域。原始研究笔记归档不再作为现行设计依据。

【分析、评价】规划回答下一步做什么，设计回答系统如何组织，实现记录回答实际完成及证据；deprecated只收被替代资料，不因文档日期较早就将仍有效的HLD或LLD判废。旧链接继续可达，但不保留两套正文。未来实施时按主题更新，阶段计划不能冒充完成记录。

【验证】迁移后的正文、导航和README共20份文件与生成的预期内容逐一比对通过；正文迁移仅作来源标记、链接重定位和本次记录补充，未删减历史章节。dev_docs及用户README的本地Markdown链接失效数为0，git diff --check通过。没有更改应用代码、环境或数据，未重复运行模型/后端/前端测试。

## 2026-09-16 · 合并研究笔记与智能提升计划

【任务】合并两份agentic-rag-static笔记，规划如何在代码中体现研究智能，并兼顾响应、token、协调、验证和做减法。本轮仅文档工作，不修改运行代码或执行模型测试。

【实现情况】agentic-rag-static.md成为唯一维护主文档；两份原始正文完整保存在agentic-rag-static-history.md，原第二篇变为导航。新增当前代码缺口、目标工作流程图、结构化证据/预算状态、模块边界、60题评估草案、分阶段交付与回退条件。同步Strata及HLD/LLD导航，明确现状与目标分离。

【分析、评价】当前系统已采用Agent研究，不是纯朴素top-k；优先问题是validate只检查来源版本，route仅在证据为空时补查，以及研究报告没有结构化交接。推荐先建立运行账本和人工核对题集，再改缺口路由；不直接叠加多Agent或昂贵Judge。“6/9/10分”保留为用户体验标尺，不虚构测量或专家等价性。所有新token与延迟阈值均标为实验初值。

【资料边界】复用已有上游源码笔记，核对本地graph/config/models实现及官方LangChain middleware、structured-output、models和LangGraph工作流文档。不推断chat-langchain远端检索算法，也不把在线新API默认视为当前已安装版本兼容。

【验证】归档内容与读取时两份原始正文逐一比对（统一换行后）均完整保留；主文档本地链接无失效，代码围栏成对；本轮相关文件git diff --check通过。未修改运行代码，因此未重复后端和前端测试，未执行真实模型调用、性能基线或网页端对照；文档中的晋级阈值不是测试结果。

## 2026-09-15 · 稳定性回归与 HLD / LLD 落盘

【任务】继续完善启动后可观察状态与接口保护，维护系统设计、模块设计和工作流程。非目标：更换技术栈、迁移数据、代替用户进行真实模型效果对照。停止条件为相关确定性回归通过、设计与实现一致、未验证项明确记录。

【实现情况】健康检查改用 SQLite COUNT(*)，避免每次轮询反序列化整库。初始化期间聊天返回503及Retry-After，官方更新、本地导入和网页确认返回409，避免重复准备与写入竞争。初始化后空库进入error而非假就绪；官方初始化异常同步结束任务状态，避免页面一直显示抓取中。新增独立 HLD.md / LLD.md，包含架构、启动工作流、状态图、模块责任、数据事实来源、错误契约及已知限制；同步 API_PIPELINE、README、Strata，保留既有说明。

【验证】本轮后端全套30项测试通过，src/tests 的ruff检查通过；Bash和Zsh启动脚本语法检查通过。新增覆盖准备期门禁、轻量健康接口、后台准备成功/空库/异常。最初新增测试的导入排序检查失败，调整排序后检查通过。前端代码本轮未改，未重复上轮已通过的构建与流协议检查。

【分析、评价】把“HTTP在线”“知识库可用”“模型配置存在”分开，避免加载过程看起来像聊天故障。测试使用临时数据库与替身导入，不修改真实文档或调用付费模型。当前官方更新后首次检索仍可能触发增量索引；线程内embedding不能由取消异步任务立即终止；这些是明确保留的限制，不宣称已经实现任务队列或无等待热更新。

【运行边界】本轮检查时18765没有监听结果，健康请求未取得响应；未据此推断进程退出原因，也没有强制启动完整CPU索引或终止其他程序。本轮不宣称真实服务持续在线、完整Qwen索引完成或真实DeepSeek前端问答通过。实际启动仍使用 PORT=18765 bash launch.sh（或zsh）；本轮后端改动需重启后生效。

## 启动可重复执行与前端就绪状态

【任务】修复重复启动端口冲突、每次重装包、知识库未就绪仍发送及错误信息丢失。

【实现情况】新增标准库launcher.py，启动前检查端口与本应用app_id；同应用复用地址，未知占用明确退出。按环境/清单缓存依赖安装标记，UPDATE_DEPS可强制更新。前端增加加载状态卡片、向量进度、自动重连、发送就绪门禁；HTTP错误保留后端detail。DenseIndex提供加载阶段和已处理片段数。

【分析、评价】之前日志中的Built来自启动时重复执行uv pip editable安装，而不是前端每次重建。本轮消除无变更的重复安装。端口冲突在终端处理，不能伪装成可达的网页提示；网页已经运行时显示真实准备状态。后台向量同步不是服务就绪，二者明确区分。

【验证】26项后端测试通过，ruff通过，前端构建和2项流协议测试通过。安装缓存与未知端口占用有回归用例。将重启此前由助手启动的服务加载新后端，不终止用户其他应用。

## 网页无法打开排查

【原因】启动前同步prepare_docs导致uvicorn未监听；发现两个Qwen CPU准备进程重复运行。另外Windows 8000端口由Cursor进程26396监听但HTTP超时。

【实现】停止两条明确识别的旧准备进程；将准备移至服务生命周期后台任务，health暴露preparation状态，前端轮询提示。未准备完成的问答返回503。launch移除同步准备并显式采用asyncio事件循环。

【验证】前端构建通过，应用与启动相关6项测试通过。使用asyncio与18765端口启动服务，从Windows实测首页200、health正常（158份文档、preparation running）。未结束Cursor程序，未宣称向量准备完成。当前可访问http://127.0.0.1:18765/。

## 启动环境遵循终端与uv

【任务】优先使用当前激活的Python虚拟环境，不在static1重复创建；安装工具统一使用uv。

【实现情况】launch.sh按VIRTUAL_ENV → STATIC1_VENV → static1/.venv → 仓库.venv选择解释器，仅没有可复用环境时执行uv venv --seed --python=3.12。依赖使用uv pip install --python明确安装到选定环境；相对环境路径在切工作目录前解析。无效的显式环境直接报错。README已同步。

【验证与评价】使用临时目录和命令替身验证激活环境（含空格路径）、父仓库环境、无环境自动创建三种分支，3项测试通过；bash/zsh语法与ruff通过。未重装用户真实环境或重新下载模型。

## 官方文档预处理与前端直接对话

【任务】让文档提前处理并保存本地，用户在浏览器直接问答。

【实现情况】新增prepare_docs.py并接入launch.sh。启动前下载官方三个Python分区、保存SQLite正文和准备报告；报告及文档版本匹配时跳过下载。配置embedding时预先同步Chroma。保留页面手动更新入口，CLI问答仅作为开发工具。

【分析、评价】把文档准备成本前移；首次启动需要等待，后续复用本地数据。真实抓取正在执行，结果随后追加。ruff与shell语法检查通过。

【落盘结果】首批已实际保存147/159篇到knowledge.sqlite3，准备报告official-preparation.json记录正文版本和12项抓取失败。检测到同域重定向与超时，补充限定官方域名的重定向处理；已有部分成功语料允许启动，不因个别失败阻塞。按当前配置，Qwen3-Embedding-0.6B已在CPU加载，整库向量同步仍在执行，尚未记录完成。

## 官方文档交流链路实现

【任务】按已沉淀的chat-langchain研究设计实现公开文档交流，保持现有技术栈；实际问答与网页端对照由用户测试。

【实现情况】新增official_docs.py，从官方三个Python分区llms.txt发现Markdown页面，限定域名与分区，四路并发抓取，复用Document/Knowledge版本入库。新增POST/GET /api/official-docs、后台进度与部分失败报告、生命周期取消。React侧栏加入更新入口，来源支持官方原文链接，对话导出包含模型服务信息和来源版本。研究增加英文技术词指引、搜索缓存、阅读复用及“必须先发现来源”约束；补查复用前轮数据，回答强调文档支持的API与代码。

【验证】21项后端测试通过，新增用例覆盖目录域名/分区限制和失败保留旧版本。ruff通过；TypeScript及Vite生产构建通过；2项SSE协议测试通过。核对公开llms.txt显示官方提供分区索引。本轮未运行实际模型问答、没有导入整批官方语料、未操作chat-langchain网页；这些体验测试留给用户。

【分析、评价】知识获取使用公开Markdown快照，不需要上游托管凭据。首次导入和向量同步耗时尚未测量；三Python分区不是完整官网，Pylon、完整LangSmith文档、持久线程未在本轮实现。更新任务重启需重试，已成功入库页面不丢失；相同来源重复更新不会新增重复文档。

## 2026-09-14 · 四层设计与上游设计沉淀

【任务】同步下层API pipeline，沉淀chat-langchain设计，保持既定技术栈，并支持zsh启动入口。

【实现情况】新增API_PIPELINE.md，按顶层架构、上层功能、下层HTTP与内部API、底层模块职责组织；包含流程图、接口表、实际错误语义、检索同步规则、上游功能映射、官方文档对照路线。更新README、IMPLEMENTATION、Strata。launch.sh在zsh入口下转交Bash执行。

【分析、评价】明确SQLite原文与Chroma索引、search定位与read证据、确定性核验与语义充分性的区别。分层方案标记为后续设计，未声称main.py和Knowledge已完成职责拆分；公开文档语料与Pylon私有支持库分别处理。新增阅读顺序、源码索引及维护约定，日常优先查沉淀文档，仅在上游升级或具体问题时回查源码。

【验收边界】本次是启动兼容修正和设计文档同步；没有执行官方文档批量采集或网页端对比，也未变更现有HTTP契约。

【执行验证】bash -n与zsh -n均通过；zsh跟踪输出确认入口执行bash launch.sh。使用故意失败的Python替身在安装前停止，退出码1符合预期；没有重跑安装或启动常驻服务。

## 2026-09-14 · 本地 embedding 与启动入口

【任务】实现 launch.sh 自动虚拟环境，以及可选 HuggingFace + Chroma dense/BM25 混合检索。

【实现情况】新增 launch.sh，复用或创建 Python3.12+ 环境、安装依赖、按需构建前端。Settings 从 .env 读取 embedding_path/device/query_prompt；空路径保留纯 BM25。DenseIndex 延迟加载本地模型与 Chroma，按模型目录/文件状态隔离集合，按片段内容和版本生成稳定ID，补齐新增向量并移除旧向量。RRF融合两路排名；API、CLI、评估入口均传入配置。

【验证】原16项回归通过；新增3项测试通过，覆盖关闭模式、RRF、真实Chroma的重复同步与旧版本清理。实际加载 E:/local_models/embedding/iic--nlp_gte_sentence-embedding_chinese-base，在临时Chroma中用“地基施工”正确将“地基基础施工”排在“设备采购”前。shell语法与src的ruff检查通过。

【分析、评价】两路检索复用统一页/行窗口，证据仍需 read_doc 核验。首次向量化会增加搜索延迟；当前采用小库同步策略，不引入后台索引服务。Qwen/large、GPU与整库混合检索评估尚未执行；未声称相对BM25准确率提升。launch.sh未实际执行安装和常驻服务流程，已验证语法及现有环境运行。

## 2026-09-14 · 接续验证与检索基线

【任务】完成中断后的验证，以最新 Strata 和统一源码为准。

【实现情况】确认9月13日已完成 React/Deep Agents 重构；停止沿用已删除的旧 app.js 检查入口。后端原15项测试通过；前端 TypeScript和生产构建通过；两项流协议测试通过；ruff检查通过。新增 src/evaluate.py 与可重复报告 reports/retrieval_v4.json，使用真实数据库检索并按版本读取正文。

【分析、评价】8个 retrieval_v4 样例的正确来源均排首位，来源Recall@6、预期正文词项覆盖率、MRR均为1.0。样例数量有限，不能推断通用检索能力或回答准确率；暂没有基于这组结果添加向量或重排的证据。

【验证边界】本次未调用真实模型、未重跑浏览器视觉检查；沿用9月13日记录中的既有验证，未宣称重复验证。reliability_v4 的歧义、缺失与字段准确率仍待独立模型评估。持久线程、云端服务与多Agent继续遵循Strata后续范围。

【新增回归】新增评估器测试通过，验证“来源命中但关键日期缺失”不会计为正文证据命中；本次共验证16项后端测试，新增文件ruff检查通过。

## 2026-09-13 · 统一源码与知识接入

按用户要求，用当前需求重写后端，不再维护旧固定页面检索链路。React 应用从 web 迁入 frontend；移除旧 app.js、styles.css、index.html 和 agent/docs.py。历史记录只用于解释演进，不是另一套可运行版本。

新增 knowledge.py（SQLite、Document/Page、成熟 BM25 库检索与按页/行阅读）、parsers.py（UTF-8、LiteParse 2.4.0、Crawl4AI 0.9.3、Firecrawl v2）、agent/graph.py（Deep Agents + LangGraph）、agent/models.py（统一模型与 tracing）。网页先预览后确认，同域名配置用户会话；默认不上传 PDF 到云解析服务。

实测：8份 v4 文本和3份 PDF 均导入成功。PDF 分别为13、9、17页；Crawl4AI 抓取 example.com 成功；Cookie 保护的本地测试页已验证 storage_state 复用。Firecrawl 仅做 HTTP 契约与错误测试，未使用真实云端密钥调用。

真实 DeepSeek 模型通过 Deep Agent 的 search_docs/read_doc 得到“南溪地基基础施工计划完成于2025年10月22日”，并返回实际读取的南溪文本来源。该测试不等同于完整数据集上的问答准确率评估。

后端15项测试通过；前端2项流协议测试、TypeScript与生产构建通过；Playwright桌面联调、移动端溢出检查通过，已查看页面截图。修正了PDF纯页码尾段错误排前与长单行文本不可读取的问题。

仍未验证：Firecrawl云端调用、真实网站登录及验证码、LlamaCloud云解析、本地模型、LangSmith云端trace落库。Managed Deep Agents部署、多Agent及持久线程不在本步骤实现范围。

## 2026-09-13 · Agentic RAG 分步升级：第一步

依据两份 agentic-rag-static 笔记和 chat-langchain 的工具流设计，新增 web/ 下的 React + TypeScript + Tailwind 应用，接入既有 FastAPI /api/chat，支持 Markdown、来源、过程状态、停止与错误展示。未完成的回答不进入下一轮上下文。新增 IMPLEMENTATION.md 记录目标结构、接口与后续步骤；当前后端仍为原固定检索链路。

验证：现有后端 9 项测试通过；前端流协议 2 项测试通过，覆盖逐字节中文分包、无完成事件的截断与后端错误。没有调用真实模型；浏览器视觉检查尚未执行。

前端依赖安装完成，已生成 package-lock.json；TypeScript 检查和 Vite 生产构建通过。

## 2026-09-12 · 需求与源码核对

【任务】依据 Strata 的 NOW 和停止条件完善个人文档助手。

【实现情况】读取本地 third_party/chat-langchain 的 instructions.md、connectors/mcp.py、ingress_guards_middleware.py 和 use-stream-handler.ts。上游通过托管 MCP 搜索并读取正文，要求技术回答有文档依据；前端区分执行阶段并允许中断。

【分析、评价】现有 static1 的固定页面短摘要、中文词项不匹配、历史无限增长和错误被当作成功保存影响核心体验。此次采用本地文档正文缓存与分块 BM25，保留个人版无登录架构；借鉴搜索→阅读→回答、输入预算和可中断流式流程。没有复制上游源码，不依赖其托管身份和 Pylon 凭据。

【验收】离线测试验证检索、历史预算、错误流、页面接口；真实文档抓取验证 Markdown 正文来源。模型付费调用与浏览器视觉检查单独记录，不以离线测试替代。

【非目标与停止条件】本阶段先稳定 LangChain 文档核心链路；Strata NEXT 的私有文件导入、通用网页导入保持待办。
