# Launch Charter

Canonical charter lives in [`rag_pdfs/strata.md`](rag_pdfs/strata.md).
This repo has a single active project (`rag_pdfs`); keep the charter next to it.

Product and agent-learning roadmap lives in
[`agent_rag_growth_roadmap.md`](agent_rag_growth_roadmap.md).

Parser/tooling notes live in [`parsers/plan.md`](parsers/plan.md). Current
startup focus is to make a reliable document parser tool that can turn source
data into PDF, layout JSON, extracted images, and image-aware Markdown before it
enters the RAG pipeline.
